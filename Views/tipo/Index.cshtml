@model IEnumerable<ProyectoWebFinal.Models.tipo>

@{
    ViewBag.Title = "Index";
}

<link rel="stylesheet" href="/Content/estilos.css" />
<h2>Tipo</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_tipo }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_tipo }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id_tipo })
        </td>
    </tr>
}

</table>
