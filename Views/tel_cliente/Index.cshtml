@model IEnumerable<ProyectoWebFinal.Models.tel_cliente>

@{
    ViewBag.Title = "Index";
}

<link rel="stylesheet" href="/Content/estilos.css" />
<h2>Teléfono Cliente</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.telefono)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cliente.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.telefono)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cliente.nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.cedula }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.cedula }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.cedula })
        </td>
    </tr>
}

</table>
