@model IEnumerable<ProyectoWebFinal.Models.lugar>

@{
    ViewBag.Title = "Index";
}

<link rel="stylesheet" href="/Content/estilos.css" />
<h2>Lugares</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.lugar1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lugar1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_lugar }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_lugar }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id_lugar })
        </td>
    </tr>
}

</table>
