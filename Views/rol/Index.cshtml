@model IEnumerable<ProyectoWebFinal.Models.rol>

@{
    ViewBag.Title = "Index";
}

<link rel="stylesheet" href="/Content/estilos.css" />
<h2>Rol</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rol1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rol1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_rol }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_rol }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id_rol })
        </td>
    </tr>
}

</table>
